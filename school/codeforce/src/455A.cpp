#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    vector<int> a;
    cin >> n;

    int tmp;
    for(int i = 0; i < n; i++) {
        cin >> tmp;
        a.push_back(tmp);
    }

    
}